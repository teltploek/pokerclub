<div class="caption-wrapper" style="display:none;">
	<h1>
	{{#if roundChosen}}
		{{season}}
	{{else}}
		
		{{#if seasonChosen}}
			Alltime leaderboard
		{{else}}
			Leaderboard
		{{/if}}

	{{/if}}
	</h1>

	{{#if roundChosen}}
	<h6>
		{{round}}

		{{#if sortEntity}}
			- sorteret efter {{sortEntity}} {{sortOrder}}
		{{/if}}
	</h6>
	{{/if}}

	{{#unless roundChosen}}
	{{#unless seasonChosen}}
	<h6 class="pull-left">
		{{season}}
	</h6>
	{{/unless}}
	{{/unless}}
</div>